const Register = () => {

    const css = {
        margin: '10px',
        padding:'10px',
        border: '5px solid yellow'
    };

    return <div style={css}>Страница 2 (Register)</div>
};

export default Register;